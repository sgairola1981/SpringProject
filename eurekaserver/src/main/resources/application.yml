spring:
          application:
             name:eureka-discovery-service

server:
  port: 8083

eureka:
  client:
   registerWithEureka: true
   fetchRegistry: true
   service-url:
      defaultZone: http://localhost:8083/eureka
    